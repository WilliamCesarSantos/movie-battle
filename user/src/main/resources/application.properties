# Spring configs
spring.application.name=movie-battle-user

# Spring cloud
spring.cloud.consul.enabled=false
spring.cloud.consul.config.enabled=false

# Spring doc
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Spring datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/movie_battle_user
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL10Dialect

# Spring JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=none

# Spring Flyway
spring.flyway.locations=classpath:db/migration

# Spring Data - Redis
spring.redis.host=localhost
spring.redis.port=6379

# Security
security.jwt.token.secret-key=abcd

# App parameters
battle-movie.finished=battle-movie-finished

# Feature toggle
# Ranking
ranking.strategy.calculate.type=percentage